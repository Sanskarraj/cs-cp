#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include<bits/stdc++.h>
using namespace std;


int main() {
vector<string>st;
    string a;
    while(i<)
    reverse(st.begin(),st.end());
    for (int i=0;i<st.size();i++){
        cout<<st[i];
    }
    return 0;
}

